# API Rest de Agendamento de Eventos (Spring Boot)

![Java](https://img.shields.io/badge/linguagem-Java_17-blue?style=for-the-badge&logo=java)
![Spring Boot](https://img.shields.io/badge/framework-Spring_Boot_3-green?style=for-the-badge&logo=spring)
![MySQL](https://img.shields.io/badge/banco-MySQL-blue?style=for-the-badge&logo=mysql)

---

## 📖 Sobre o Projeto

Este projeto é uma API RESTful desenvolvida para ser o backend do **Sistema de Agendamento de Eventos**. A aplicação permite o gerenciamento completo (CRUD: Criar, Ler, Atualizar e Deletar) de **Eventos**, **Tarefas** e **Equipes**.

Foi desenvolvido como um projeto de estudos, aplicando as melhores práticas de desenvolvimento de APIs com Spring Boot, como o uso de DTOs (Data Transfer Objects) para a troca de informações, o padrão Repository com Spring Data JPA para acesso ao banco de dados e a separação de responsabilidades em Controllers e Services.

Esta API serve como backend para o [Aplicativo Frontend em React Native](https://github.com/Arlan-O/Projeto_React_Native.git).

---

## 🛠️ Tecnologias Utilizadas

As seguintes ferramentas e tecnologias foram usadas na construção do projeto:

* **Java 17:** Versão da linguagem de programação.
* **Spring Boot 3:** Framework principal para a criação da API.
* **Spring Data JPA:** Para persistência de dados e comunicação com o banco.
* **Spring Validation:** Para validação dos dados de entrada (DTOs).
* **Maven:** Para gerenciamento de dependências do projeto.
* **MySQL:** Banco de dados relacional para armazenamento dos dados.
* **Insomnia/Postman:** Para testes e documentação dos endpoints.

---

## 🚀 Como Rodar o Projeto Localmente

Siga estes passos para configurar e executar a API em sua máquina local.

### 1. Pré-requisitos

* [Java JDK 17](https://www.oracle.com/java/technologies/javase/jdk17-archive-downloads.html)
* [Maven](https://maven.apache.org/download.cgi)
* Um gerenciador de banco de dados (como MySQL Workbench, DBeaver, etc).
* Um banco de dados **MySQL** criado (veja passo 3).

### 2. Instalação

1.  **Clone o repositório:**
    ```bash
    git clone https://github.com/Arlan-O/Projeto_Agendamento.git
    ```

2.  **Acesse a pasta do projeto:**
    ```bash
    cd Projeto_Agendamento
    ```

3.  **Instale as dependências (Maven):**
    (O Maven geralmente baixa as dependências automaticamente ao abrir em uma IDE como IntelliJ ou Eclipse, ou ao rodar o comando de execução).

### 3. Configuração do Banco de Dados

1.  **Crie o banco de dados:**
    Execute o seguinte comando no seu MySQL para criar o banco que a API usará:
    ```sql
    CREATE DATABASE db_sistema;
    ```

2.  **Configure a conexão:**
    Abra o arquivo `src/main/resources/application.properties` e configure-o com suas credenciais do MySQL:

    ```properties
    # Configuração do Servidor
    server.port=8080

    # Configuração do Banco de Dados (MySQL)
    spring.datasource.url=jdbc:mysql://localhost:3306/db_sistema
    spring.datasource.username= -> Por padrão o usuário é 'root', depende das suas configurações locais. 
    spring.datasource.password= -> Por padrão a senha é vazia, depende das suas configurações locais.
    
    # Configuração do Hibernate (JPA)
    # 'update': atualiza o schema (bom para dev), use 'validate' ou 'none' em produção.
    spring.jpa.hibernate.ddl-auto=update
    spring.jpa.show-sql=true
    spring.jpa.properties.hibernate.format_sql=true
    ```

### 4. Executando a API

1.  **Execute o projeto:**
    Você pode rodar o projeto diretamente pela sua IDE (IntelliJ, Eclipse) ou via terminal com o Maven:
    ```bash
    mvn spring-boot:run
    ```

2.  **Pronto!**
    A API estará rodando em `http://localhost:8080`.

---

## 🗄️ Estrutura do Banco de Dados (SQL)

Abaixo está o comando SQL para criar a tabela principal `eventos`, baseado na estrutura utilizada no projeto.

> **Nota:** Se você configurar o `spring.jpa.hibernate.ddl-auto=update` ou `create` no `application.properties`, o Spring Data JPA criará esta tabela automaticamente. Esta documentação serve de referência.

```sql
CREATE TABLE eventos (
    id BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    nome_evento VARCHAR(255) NOT NULL,
    data TIMESTAMP,
    numero_convidados INT NOT NULL,
    local VARCHAR(255) NOT NULL,
    logradouro VARCHAR(255),
    numero VARCHAR(20),
    cep VARCHAR(10),
    cidade VARCHAR(100),
    uf CHAR(2)
);
```

---

## 📡 Endpoints da API (Exemplos Postman)
A API oferece os seguintes endpoints para manipulação dos dados. Assumimos que o servidor está rodando em http://localhost:8080.

## 📍 Eventos (/eventos)


#### ➕ POST (Cadastrar Evento)
Cria um novo evento no banco de dados.

Endpoint: `POST /eventos`

**Body (JSON):**

```JSON

{
    "nome_evento": "Festa Teste",
    "data": "2025-10-05",
    "numero_convidados": "12",
    "endereco": {
        "local": "Praça Peladão",
        "logradouro": "São João",
        "numero": "12",
        "cep": "12345678",
        "cidade": "Cidade Exemplo",
        "uf": "SP"
    }
}
```
**Resposta:** `200 OK (ou 201 Created)`.

---
## 🔍 GET (Buscar Eventos)

Busca um ou todos os eventos.

**Endpoint (Listar Todos):** `GET /eventos`

**Endpoint (Buscar por ID):** `GET /eventos/1`

**Body: (Nenhum, apenas retorna o corpo em formato json)**

### Resposta (Exemplo de GET /eventos/1):

```JSON

{
    "id": 1,
    "nome_evento": "Festa Teste",
    "data": "2025-10-05",
    "numero_convidados": 12,
    "endereco": {
        "local": "Praça Peladão",
        "logradouro": "São João",
        "numero": "12",
        "cep": "12345678",
        "cidade": "Cidade Exemplo",
        "uf": "SP"
    }
}
```
---
## ✏️ PUT (Atualizar Evento)
Atualiza um evento existente. (Requer o id no corpo).

**Endpoint:** `PUT /eventos`

Body (JSON):

```JSON

{
    "id": 1,
    "nome_evento": "Festa Atualizada",
    "data": "2025-10-06",
    "numero_convidados": "50",
    "endereco": {
        "local": "Novo Local",
        "logradouro": "Rua Nova",
        "numero": "100",
        "cep": "12345678",
        "cidade": "Cidade Exemplo",
        "uf": "SP"
    }
}
```
**Resposta:** 200 OK.

---
## ❌ DELETE (Deletar Evento)
Remove um evento pelo seu id.

**Endpoint:** `DELETE /eventos/1`

**Body: (Nenhum)**

### Resposta: 204 No Content (Sucesso, sem corpo).
